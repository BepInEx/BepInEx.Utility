name: ci

on:
  push:
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        os: [windows-latest]
    steps:
      - uses: actions/checkout@v2
      - name: Dotnet Build
        run: |
          dotnet build -c Release
      - uses: actions/upload-artifact@v2
        with:
          name: plugins-all-in-one
          path: bin/**/*.dll
          if-no-files-found: error
      - name: Package
        run: |
          pwsh release.ps1
      - uses: actions/upload-artifact@v2
        with:
          name: plugins
          path: bin/*.zip
          if-no-files-found: error
